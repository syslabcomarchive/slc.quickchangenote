Introduction
============

slc.quickchangenote hijacks the Change Note field provided by cmfeditions,
and provides an auto-complete text field that draws its values from
a list of configured text values. This is useful for people who frequently
make the same sorts of changes to documents.
